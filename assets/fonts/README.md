# Fonts Directory

Place your font files here (.ttf, .otf, etc.)

## Supported Formats
- TrueType (.ttf)
- OpenType (.otf)

## Usage
Fonts can be loaded using the ResourceManager:
```cpp
auto* font = resourceManager.loadFont("assets/fonts/Arial.ttf", 16);
```
